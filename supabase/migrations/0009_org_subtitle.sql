-- ============================================================================
-- 겸직 표시
--
-- 이사 중 한 명이 감사를 겸한다.
-- 칸을 따로 만들지 않고 이름 옆에 작은 배지로 붙이기 위해 보조 직책을 둔다.
-- ============================================================================

alter table org_members
  add column if not exists sub_title text;

comment on column org_members.sub_title is
  '겸직 표시. 예: 이사이면서 감사이면 title=이사, sub_title=감사';
