-- ============================================================================
-- 조직 분류 변경 — 고문단 / 자문위원 대신 역대 회장
--
-- enum 값은 지울 수 없으므로 새 값만 더한다.
-- 기존 고문단·자문위원 행은 아래에서 정리한다.
-- ============================================================================

alter type org_group add value if not exists '역대 회장';
