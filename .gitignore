node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
Thumbs.db

# 임원 사진 원본은 올리지 않는다 (저장소에 업로드된 뒤에는 필요 없다)
photos/*
!photos/README.md

# 로고넣기 폴더에 떨어뜨린 원본. npm run logo 로 public/logo 에 복사되므로
# 저장소에 두 벌 가질 필요가 없다. 안내문만 남긴다.
로고넣기/*
!로고넣기/읽어주세요.txt
